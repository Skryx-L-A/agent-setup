## Related

- Verify outcomes yourself (run tests / exercise the change) after deciding.
- Persist important decisions as a note in `~/Knowledge` (typed wikilinks).
- Gather external references (web search) before convening if the decision needs facts.
- Formalize long-lived outcomes with the `architecture` skill (ADR).

