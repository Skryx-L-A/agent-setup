## Die Electron-Werkbank auf peer vom Mac aus neu starten (2026-09-08, gemessen)

Sie läuft in Hyprland-Sitzung des Nutzers (Wayland), gestartet als
`./node_modules/electron/dist/electron . --show` in `~/AI/claude-workbench/app`, nach einem
Neustart mit Elternprozess `init`. Der Weg, der funktioniert hat: Hauptprozess ermitteln über den
Renderer (`pgrep -f "[a]pp-path=/home/<user>/AI/claude-workbench/app"`, dann dessen `ppid`; die
eckige Klammer verhindert, dass `pgrep` die eigene Shell findet), `kill <main>`, dann
`systemd-run --user --unit=werkbank-electron-<zeit> --setenv=WAYLAND_DISPLAY=wayland-1
--setenv=XDG_RUNTIME_DIR=/run/user/1000 --setenv=XDG_SESSION_TYPE=wayland
--setenv=HYPRLAND_INSTANCE_SIGNATURE=<aus /proc/<main>/environ> --working-directory=/home/<user>/AI/claude-workbench/app
/home/<user>/AI/claude-workbench/app/node_modules/electron/dist/electron . --show` — ABSOLUTER Pfad
zur Binärdatei, `systemd-run` löst `./…` nicht auf. `hyprctl dispatch exec "…"` scheiterte an der
neuen Lua-Syntax von hyprctl („')' expected near 'bash'“), nicht weiter verfolgt. Vorher prüfen,
dass dort keine Worker laufen (Seitenleiste peer „0 Sitzungen“); die tmux-Sitzungen überleben den
Neustart, nur die Oberfläche startet neu.

