## Text mit Backticks nie durch eine Shell-Ebene schicken (2026-07-30)

In `ssh '…python3 -c "…"'` führt die entfernte Shell Backticks als Command-Substitution aus und
ersetzt sie durch Leerstellen. Das hat eine fertige Markdown-Datei stillschweigend zerstört,
während der Schreibvorgang Erfolg meldete — der Rückgabewert war 0, der Inhalt war weg.

Richtig ist: Skript per `Write` lokal anlegen, mit `scp` übertragen, dort ausführen. Danach die
Zieldatei auf leere Backtick-Paare prüfen, statt dem Exit-Code zu glauben.

Gilt für jede Shell-Ebene, nicht nur für `ssh` — auch lokal in `bash -c "…"` und in jedem
Kommando, dessen Text durch eine weitere Auswertung läuft.

Hergang: [[session-2026-07-29-30-verlustanalyse]].

- **Peer-Rechner-Zusätze seit dem Omarchy-Umzug (2026-08-25, Vorgaben des Nutzers über die
  Omarchy-Einrichtungssession):** (a) GUI-Tests von Agenten laufen auf **Hyprland-Workspace 6**
  — dort ist alles erlaubt (Apps, Vollbild-Screenshots), isoliert von Sitzung des Nutzers; der
  Weg über verschachtelte/headless Compositors bleibt der Standard für Automatisches.
  (b) Projekte ziehen in den Omarchy-Projects-Ordner um (Zielpfad klärt die
  Einrichtungssession; bis dahin keine eigenen Umzüge anstoßen). (c) Prozess-Hygiene dort
  besonders streng: es läuft nur, was gerade gebraucht wird — Trader nur zur Handelszeit,
  nachts nichts Stehendes; aktive Bau-Läufe im Auftrag sind davon unberührt, hinterlassen
  aber nichts.

