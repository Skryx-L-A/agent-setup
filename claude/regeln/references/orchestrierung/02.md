## Modellwahl

- **Registry-Kennung löst auf peer nicht auf (2026-09-10):** ein `wb-consistency`-Fund
  VERWEIS-INS-LEERE für eine Modell-Kennung heißt meist, dass die Registries nicht abgeglichen
  sind; vor der Fehlersuche `wb-registry-sync peer --dry-run` (Regel in `regeln/maschinen.md`).
: ein Ort für alle Regeln (zusammengeführt 2026-08-28)

Dieser Abschnitt ist der einzige Ort, an dem steht, wann ein Worker welches Modell und welche
Denkstufe bekommt. `~/.claude/CLAUDE.md`, `roles/orchestrator.md` und `roles/agent.md` verweisen
hierher. Die maschinell erzeugte Registry-Tabelle in `roles/orchestrator.md` ergänzt diesen
Abschnitt um die Eignung jedes einzelnen Eintrags; sie ersetzt ihn nicht.

